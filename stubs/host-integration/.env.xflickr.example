# XFlickr Crawler — add to host app .env
# Uses the same Redis as the host queue and cache (no separate infrastructure).

QUEUE_CONNECTION=redis
CACHE_STORE=redis

XFLICKR_QUEUE=xflickr
XFLICKR_DEFAULT_APP_PROFILE=main
XFLICKR_MAX_REQUESTS_PER_HOUR=3300
